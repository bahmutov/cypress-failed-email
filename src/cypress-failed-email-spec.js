'use strict'

/* global describe, it */
describe('cypress-failed-email', () => {
  const cypressFailedEmail = require('.')
  it('write this test', () => {
    console.assert(cypressFailedEmail, 'should export something')
  })
})
